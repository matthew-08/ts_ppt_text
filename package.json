{
  "name": "ts_ppt_text",
  "version": "1.2.3",
  "description": "A simple TypeScript library for interfacing with PPT text nodes",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx tsnd ./src/test.ts",
    "build": "babel src --out-dir dist --source-maps"
  },
  "author": "Matthew",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^18.16.3",
    "fast-xml-parser": "^4.2.2",
    "ts-node-dev": "^2.0.0"
  }
}
